import { Pressable, Text } from "react-native";

export const CadastroButton = ( { onPress } ) => {
    return (
        <Pressable 
            onPress={onPress}
        >
            <Text>asfaf</Text>
        </Pressable>
    )
}